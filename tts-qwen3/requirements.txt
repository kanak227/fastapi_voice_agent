fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.8.0
# qwen-tts==0.1.1 hard-pins transformers==4.57.3 and we keep that — it's the
# version the model was published against. The original "missing
# preprocessor_config.json" crash was caused by HF_HOME + TRANSFORMERS_CACHE
# both set to /models, splitting the cache into /models/... and /models/hub/...
# (only the latter had the file). The Dockerfile now sets HF_HOME only.
#
# torch 2.12+ has a `Duplicate dispatch rule for sys.intern` regression in
# torch._dynamo polyfills. torch 2.7.1 is the newest stable before that.
torch==2.7.1
torchaudio==2.7.1
torchvision==0.22.1
# qwen-tts is the upstream package (pulled in transitively by
# faster-qwen3-tts, pinned here for reproducibility). faster-qwen3-tts adds a
# CUDA-graph runtime + true streaming on top, cutting time-to-first-audio from
# 6-40s to ~680ms on a T4 and reaching real-time RTF. Validated working with
# torch 2.7.1 (CUDA graphs need torch>=2.5.1).
qwen-tts==0.1.1
faster-qwen3-tts>=0.2.6
soundfile>=0.13.0
# numpy 1.26.x is the last version compatible with both pandas 1.5 (from TTS)
# and torch 2.7. numpy 2.x breaks pandas 1.5's C extensions.
numpy>=1.26.0,<2.0
uroman>=1.3.1
indic-transliteration>=2.3.62
# AI4Bharat FastPitch + HiFiGAN for Indian languages (Coqui TTS runtime).
# Fully parallel, RTF < 1.0 on CPU, handles punctuation natively.
# Install TTS BEFORE transformers so its older pin gets overridden by qwen-tts's.
TTS>=0.22.0
